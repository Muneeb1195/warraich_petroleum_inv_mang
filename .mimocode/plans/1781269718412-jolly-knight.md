# Plan: Fix Linux CI build + center nav icons

## 1. Linux build missing gtk+-3.0
**File:** `.github/workflows/build.yml`
- Add step before build: `sudo apt-get update && sudo apt-get install -y libgtk-3-dev`

## 2. Center NavigationRail icons on desktop
**File:** `lib/screens/home_shell.dart`
- Add `NavigationRail` label type and center alignment

## Files to modify
- `.github/workflows/build.yml`
- `lib/screens/home_shell.dart`
