# Plan: Fix silent cloud restore failure

## Root cause
The cloud restore silently fails — no error shown. The issue is likely:
1. `listCloudBackups()` returns empty (backup dialog never opens) OR
2. `restoreCloudBackup()` returns false but snackbar doesn't show

## Fix
1. Add `dart:developer` log to backup_screen.dart cloud restore tap handler
2. Add visible snackbar feedback for every step of the restore
3. Show "No backups found" vs "Restore failed" vs success

## File to modify
- lib/screens/settings/backup_screen.dart — add logging and better error feedback to _showCloudRestoreDialog and cloud restore tap handler

## Verification
- Tap Cloud Restore — should show dialog with backups list
- Select a backup — should show progress or error
- Check device logs via error logger in Settings
