# Plan: Fix CI Build Failures

## Problem
`lib/config/app_config.dart` is in `.gitignore` so CI doesn't have it. `backup_service.dart` imports it, causing build failure:
```
Error: Error when reading 'lib/config/app_config.dart': No such file or directory
```

## Fix
1. Remove `app_config.dart` from `.gitignore`
2. Replace hardcoded secrets in `app_config.dart` with empty string defaults (real values come from `--dart-define` at build time)
3. Commit and push to re-trigger CI

## Files to modify
- `.gitignore` — remove `app_config.dart` line
- `lib/config/app_config.dart` — replace secret defaults with empty strings

## Verification
- `flutter analyze` — 0 errors
- Push triggers CI which should now compile
